<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyFoodApplication"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".ChineseitemActivity" />
        <activity android:name=".IndianitemActivity" />
        <activity android:name=".ContactActivity" />
        <activity android:name=".ItalianActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".ThaiitemActivity" />
        <activity android:name=".Cart" />
        <activity android:name=".CartItem" />
        <activity android:name=".CartActivity" />
        <activity android:name=".PaymentSuccessActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".Cart_layoutActivity">
        </activity>


    </application>

</manifest>



